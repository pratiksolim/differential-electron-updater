version: 1
retry:
  max_attempts: 3
  cooldown_seconds: 5

retryable_patterns:
  - name: "simulated network failure"
    pattern: "SIMULATED_NETWORK_FAILURE"
    is_regex: false
    strategy: "rerun-failed-jobs"
